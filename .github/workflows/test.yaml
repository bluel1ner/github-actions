name: Demo workflow

on: workflow_dispatch

jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print
        run: echo Hello GH actions
      - name: Print a few lines
        run:
          echo First line!
